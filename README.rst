===========
Git-ja 
===========

Git utilities for gitjas. 

Currently git-ja has 3 simple functions:

git ja show-tracking
====================

Will show which branch is tracking which remote branch

git ja promote
===============

Will send the current branch to the tracking remote. If none is defined it will default to origin

git ja divergence
=================

Will show a divergence graph between the specified branches
